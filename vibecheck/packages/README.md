# Extension packages

Local VSIX builds are written to this directory by `npm run package:vsix`.

The binary artifacts are ignored by Git. Install the current build with the versioned command in
the project README or development guide.
